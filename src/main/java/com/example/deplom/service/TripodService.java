package com.example.deplom.service;

import com.example.deplom.DTOS.TripodDTO;

public interface TripodService {
    public void createTripod(TripodDTO tripodDTO);
    void update(TripodDTO tripodDTO);
}
